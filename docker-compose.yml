services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: verbis
      POSTGRES_PASSWORD: verbis
      POSTGRES_DB: verbis
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
